import { HighlightStatus } from './highlight-status';

describe('HighlightStatus', () => {
  it('should create an instance', () => {
    const directive = new HighlightStatus();
    expect(directive).toBeTruthy();
  });
});
